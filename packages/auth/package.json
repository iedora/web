{
  "name": "@iedora/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Iedora auth surface \u2014 better-auth instance, Drizzle schema, and the role/scope access-control taxonomy shared by every iedora product.",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./client": "./src/client.ts",
    "./next": "./src/next.ts",
    "./permissions": "./src/permissions.ts",
    "./scopes": "./src/scopes.ts",
    "./audit": "./src/audit.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsgo --noEmit",
    "lint": "eslint src --cache --cache-location node_modules/.cache/.eslintcache",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "better-auth": "^1.6.11",
    "drizzle-orm": "catalog:",
    "postgres": "^3.4.9",
    "server-only": "^0.0.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@iedora/eslint-config": "workspace:*",
    "@types/node": "^22.19.19",
    "drizzle-kit": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
