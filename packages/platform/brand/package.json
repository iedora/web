{
  "name": "@iedora/brand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Iedora brand constants + cross-origin URL helpers shared across every product (apex, menu, future). Pure strings + URL builders \u2014 no runtime, no env validation, safe to import from server and client.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "lint": "eslint src --cache --cache-location node_modules/.cache/.eslintcache",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@iedora/eslint-config": "workspace:*",
    "@types/node": "^22.19.19",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
