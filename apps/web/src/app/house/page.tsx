/**
 * iedora.com landing — host-rewritten by `apps/web/src/proxy.ts`
 * (`iedora.com/*` → `/house/*`). Page lives in
 * `@iedora/product-house` so the house product's surface is
 * self-contained and lints / tests independently.
 */
export { default, metadata } from '@iedora/product-house'
